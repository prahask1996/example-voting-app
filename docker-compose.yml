version: "2"
services:
    redis:
        image: redis
        networks:
            - back-end
    db:
        image: postgres:9.4
        environment:
            - POSTGRES_HOST_AUTH_METHOD=trust
        networks:
            - back-end    
    vote:
        image: prahaskattimani/vote-app
        ports:
            - 5000:80
        depends_on:
            - redis
        networks:
            - back-end
            - front-end    
    results:
        image: prahaskattimani/result-app
        ports:
            - 5001:80
        depends_on:
            - redis
            - db
        networks:
            - back-end
            - front-end     
    worker:
        image: prahaskattimani/worker-app
        depends_on:
            - redis
            - db
        networks:
            - back-end
            - front-end     
networks:
    front-end:
    back-end:
